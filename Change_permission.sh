#to change the access control
chmod +x(r)(w)  <filename> #for add execution, read, write permisson respectively
chmod -x(r)(w)  <filename> #for deleting the execution, read, write permission respectively
#you can view by using
ls -lrta
